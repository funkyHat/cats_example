from setuptools import setup

# Replace the place holders with values for your project

setup(

    name='cat_rescue',

    version='0.1',

    py_modules=[
        'cat_rescue',
    ],

    install_requires=[
        'factory_boy',
    ],
)
